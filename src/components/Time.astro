---
import format, { formats } from "#mods/datetime/index.js";

const { date, ...attribs } = Astro.props;
const displayDate = format(date);
const isoDate = format(date, formats.iso);
---

<time datetime={isoDate} {...attribs}>{displayDate}</time>
