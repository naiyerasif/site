extends: existence
message: "Form a negative number with a minus sign, not a hyphen."
level: error
nonword: true
action:
  name: edit
  params:
    - replace
    - '-'
    - '−'
tokens:
  - '\s-\d+\s'
